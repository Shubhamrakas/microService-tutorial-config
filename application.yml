spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: default
    
eureka:
 instance:
   prefer-ip-address: true
 client:
   fetch-registry: true
   register-with-eureka: true
   service-url:
     defaultZone: http://localhost:9090/eureka
